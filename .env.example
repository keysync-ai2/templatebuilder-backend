# Neon PostgreSQL connection string
DATABASE_URL=postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/dbname?sslmode=require

# JWT signing secret (generate with: python -c "import secrets; print(secrets.token_hex(32))")
JWT_SECRET=change-me-to-a-random-secret

# DeepSeek API key (https://platform.deepseek.com)
DEEPSEEK_API_KEY=sk-4137565701a844ac87090c3770b36ca0

# S3 bucket name (created by SAM template)
S3_BUCKET=email-builder-assets-123456789012

# CloudFront distribution domain (created by SAM template)
CF_DISTRIBUTION_DOMAIN=d1234abcdef.cloudfront.net

# CORS origin (your frontend URL)
CORS_ORIGIN=https://your-app.amplifyapp.com
